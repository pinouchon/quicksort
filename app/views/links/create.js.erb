<% if @link.valid? %>
//flash('notice', 'Link');
var $newLink = $('<%= j render partial: 'links/link', locals: {link: @link} %>');
$('#links').append($newLink.fadeIn());
$('#links').append('<hr class="nomargin"/>');
$('.link').removeClass('selected');
$('#link-' + <%= @link.id %>).addClass('selected');
$('body').animate({
    scrollTop: $('#link-' + <%= @link.id %>).offset().top
}, 1000);
<% @link = Link.new %>

<% else %>

<% end %>
$('#new_link').replaceWith('<%= j render partial: 'links/form' %>');
$("#my_wmd").wmd();
