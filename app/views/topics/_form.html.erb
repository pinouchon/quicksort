<% remote ||= false %>
<!-- BEGIN FORM-->
<%= simple_form_for(@topic,
                    :html => {:class => 'form-inline'}, remote: remote) do |f| %>
    <%= f.input :name, label: 'Title', input_html: {class: 'span12'} %>

    <div class="control-group">
      <!--<label class="control-label"></label>-->
      <div class="controls">
        <%= f.text_area :description, class: 'span12 m-wrap wmd-input my_wmd', rows: '6' %>
        <!--<textarea class="span12 m-wrap wmd-input" id='my_wmd' rows="6"></textarea>-->
      </div>
    </div>

    <div class="form-actions">
      <%= button_tag(type: 'submit', class: "btn blue") do %>
          <i class="icon-ok"></i> Create</button>
      <% end %>

      <a href='<%= url_for :back %>' class='btn cancel'>
        Cancel
      </a>


      <!--<button type="submit" class="btn blue"><i class="icon-ok"></i> Save</button>-->
      <!--<button type="button" class="btn">Cancel</button>-->
    </div>

<% end %>
